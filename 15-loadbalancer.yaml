apiVersion: v1
kind: Service
metadata: 
  name: nginx-la
spec:
  type: LoadBalancer
  selector:
    project: roboshop
    environment: dev
    component: frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 31299
    